# Deployer private key — never commit the real value
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# RPC endpoint for the target network
RPC_URL=https://mainnet.infura.io/v3/YOUR_INFURA_KEY

# Protocol treasury address — receives the protocol cut of fees
TREASURY_ADDRESS=0xYourTreasuryAddressHere

# Protocol fee in basis points (max 3000 = 30% of the market fee)
# Example: 1000 = 10% of the market fee goes to the protocol treasury
PROTOCOL_FEE_BPS=1000

# Optional: Etherscan / block-explorer API key for --verify
ETHERSCAN_API_KEY=YOUR_ETHERSCAN_API_KEY
